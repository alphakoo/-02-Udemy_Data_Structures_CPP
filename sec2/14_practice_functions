#include <iostream>
using namespace std;

void func(int &, int &);

int main() {

	int x=10, y=20;

	printf("(x, y) >> (%d, %d)\n", x, y);
	
	func(x, y);

	printf("(x, y) >> (%d, %d)\n", x, y);
	
}

void func(int &num1, int &num2){

	num1 = 100;
	num2 = 200;
}
